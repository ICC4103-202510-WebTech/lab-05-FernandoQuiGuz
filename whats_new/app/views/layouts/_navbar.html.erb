<nav class="navbar navbar-dark bg-dark mb-4">
  <div class="container d-flex justify-content-between align-items-center">
    <a class="navbar-brand text-white" href="#">WhatsNew</a>

    <div class="d-flex gap-2 align-items-center">
      <%= link_to 'Users', users_path, class: 'btn btn-primary' %>
      <%= link_to 'Chats', chats_path, class: 'btn btn-success' %>
      <%= link_to 'Messages', messages_path, class: 'btn btn-warning' %>

      <% if user_signed_in? %>
        <span class="navbar-text text-white ms-3 me-2">
          Signed in as <strong><%= current_user.email %></strong>
        </span>
        <%= button_to 'Sign out', destroy_user_session_path, method: :delete, form: { class: 'd-inline' }, class: 'btn btn-outline-danger btn-sm' %>
      <% else %>
        <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-primary btn-sm' %>
        <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-outline-success btn-sm' %>
      <% end %>
    </div>
  </div>
</nav>
